using System;

namespace num2
{
    class Accauntat
    {
            public bool AskForBonus(Post worker, int hours)
            {
                return (hours <= Convert.ToInt32(worker));
            }
    }
}
